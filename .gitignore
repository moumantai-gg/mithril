bin/
obj/
.vs/
*.user
*.suo
TestResults/
artifacts/
publish/
releases/
tests/.tmp/

# Temp files for Claude
src/temp
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/tmp/
.claude/worktrees/
reviews/

# Raw log captures + local scratch (per scratch_data_location_not_tmp memory)
scratch/

# Legacy browser-based garden helper (superseded by Samwise module)
garden/

# Continue
.continue/agents/local-env.yaml

# Worktrees
worktrees/

# Repo-root scratch screenshots (e.g. test inputs for tools/MapCalibrationFromScreenshot)
/*.png
/*.jpg

# Visual-inspection PNG sidecars written by --phase emit-templates alongside the
# committed icon-templates.{json,bin}; regenerated on demand, never embedded.
/src/Mithril.MapCalibration/BundledData/*.png
.superpowers/
