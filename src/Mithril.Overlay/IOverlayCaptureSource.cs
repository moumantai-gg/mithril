// IOverlayCaptureSource was moved to Mithril.MapCalibration.Detection (#1095)
// so that LiveMapViewService can reference it without creating a circular
// project dependency (Mithril.Overlay already references Detection).
// The type is now Mithril.MapCalibration.Detection.IOverlayCaptureSource;
// OverlayWindowCaptureSource implements that interface directly.

