<UserControl x:Class="Mithril.Shell.Views.DiagnosticsSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:views="clr-namespace:Mithril.Shell.Views"
             Background="#FF1A1A1A">
    <UserControl.Resources>
        <ResourceDictionary Source="pack://application:,,,/Mithril.Shared.Wpf;component/Converters.xaml"/>
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24,16" MaxWidth="640" HorizontalAlignment="Left">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <icon:PackIconLucide Kind="Activity" Width="22" Height="22"
                                     VerticalAlignment="Center" Margin="0,0,10,0"
                                     Foreground="#FFD4A847"/>
                <TextBlock Text="Diagnostics"
                           FontFamily="{DynamicResource AppHeaderFontFamily}"
                           FontSize="{DynamicResource AppFontSizeHero}" FontWeight="SemiBold"
                           Foreground="#FFD4A847" VerticalAlignment="Center"/>
            </StackPanel>

            <TextBlock Text="Performance trace" Foreground="#88FFFFFF" Margin="0,0,0,4" FontWeight="SemiBold"/>
            <TextBlock TextWrapping="Wrap" Foreground="#88FFFFFF"
                       FontSize="{DynamicResource AppFontSizeHint}" Margin="0,0,0,8"
                       Text="Records frame timing, the WPF dispatcher queue, GC pressure, binding errors, and module-activation costs to a JSON-lines file. Used to investigate UI sluggishness — leave off unless you're collecting a trace."/>

            <CheckBox Content="Enable perf-trace harness"
                      IsChecked="{Binding Settings.EnablePerfTrace, Mode=TwoWay}"
                      Foreground="#FFE8E8E8" Margin="0,0,0,8"/>

            <CheckBox Content="Auto-start a session at shell launch (captures startup-time perf)"
                      IsChecked="{Binding Settings.AutoStartPerfTrace, Mode=TwoWay}"
                      IsEnabled="{Binding Settings.EnablePerfTrace}"
                      Foreground="#FFE8E8E8" Margin="0,0,0,8"/>

            <CheckBox Content="Emit a frame event per render (verbose; larger files)"
                      IsChecked="{Binding Settings.VerboseFrameEvents, Mode=TwoWay}"
                      IsEnabled="{Binding Settings.EnablePerfTrace}"
                      Foreground="#FFE8E8E8" Margin="0,0,0,16"/>

            <TextBlock TextWrapping="Wrap" Foreground="#66FFFFFF"
                       FontSize="{DynamicResource AppFontSizeHint}" Margin="0,0,0,4"
                       Text="Sessions land in:"/>
            <TextBlock TextWrapping="Wrap" Foreground="#AAFFFFFF"
                       FontSize="{DynamicResource AppFontSizeHint}"
                       FontFamily="Consolas, Cascadia Mono"
                       Text="{Binding PerfDirectoryHint}" Margin="0,0,0,12"/>

            <TextBlock TextWrapping="Wrap" Foreground="#88FFFFFF"
                       FontSize="{DynamicResource AppFontSizeHint}" Margin="0,8,0,4"
                       Text="Bind a hotkey to 'Toggle perf-trace recording' under Hotkeys settings to start/stop a session on demand. The status bar shows a red REC dot while a session is active."/>

            <!-- Map-calibration diagnostic bundles (#984) -->
            <Border Margin="0,24,0,0" Padding="0,16,0,0" BorderThickness="0,1,0,0" BorderBrush="#22FFFFFF">
                <StackPanel>
                    <TextBlock Text="Map-calibration diagnostics" Foreground="#88FFFFFF" Margin="0,0,0,4" FontWeight="SemiBold"/>
                    <TextBlock TextWrapping="Wrap" Foreground="#88FFFFFF"
                               FontSize="{DynamicResource AppFontSizeHint}" Margin="0,0,0,8"
                               Text="Writes a per-attempt diagnostic bundle (screenshot, ECC crop, deviation map, detections, recovered calibration) for each calibration attempt. A debug aid for investigating solve failures — leave off otherwise."/>

                    <CheckBox Content="Save calibration diagnostics on each attempt"
                              IsChecked="{Binding Settings.DumpCalibrationBundles, Mode=TwoWay}"
                              Foreground="#FFE8E8E8" Margin="0,0,0,16"/>

                    <TextBlock TextWrapping="Wrap" Foreground="#66FFFFFF"
                               FontSize="{DynamicResource AppFontSizeHint}" Margin="0,0,0,4"
                               Text="Bundles land in:"/>
                    <TextBlock TextWrapping="Wrap" Foreground="#AAFFFFFF"
                               FontSize="{DynamicResource AppFontSizeHint}"
                               FontFamily="Consolas, Cascadia Mono"
                               Text="{Binding CalibrationDumpDirectoryHint}" Margin="0,0,0,4"/>
                    <Button Content="Open calibration diagnostics folder"
                            Command="{Binding OpenCalibrationDumpDirectoryCommand}"
                            Padding="10,4" Margin="0,2,0,8" HorizontalAlignment="Left"/>
                </StackPanel>
            </Border>

            <TextBlock Margin="0,24,0,0" TextWrapping="Wrap" Foreground="#66FFFFFF"
                       FontSize="{DynamicResource AppFontSizeHint}"
                       Text="Changes preview live. Settings persist on shutdown."/>

            <!-- Log maintenance -->
            <Border Margin="0,24,0,0" Padding="0,16,0,0" BorderThickness="0,1,0,0" BorderBrush="#22FFFFFF">
                <StackPanel>
                    <TextBlock Text="Logs" Foreground="#88FFFFFF" Margin="0,0,0,4" FontWeight="SemiBold"/>
                    <TextBlock TextWrapping="Wrap" Foreground="#88FFFFFF"
                               FontSize="{DynamicResource AppFontSizeHint}" Margin="0,0,0,8"
                               Text="All Mithril logs live in one folder. 'Clear Mithril logs' removes only the rolling diagnostics JSON; 'Clear all logs' also removes boot/crash logs and saved performance-trace sessions. Files in use stay until restart."/>

                    <TextBlock TextWrapping="Wrap" Foreground="#66FFFFFF"
                               FontSize="{DynamicResource AppFontSizeHint}" Margin="0,0,0,4"
                               Text="Log folder:"/>
                    <TextBlock TextWrapping="Wrap" Foreground="#AAFFFFFF"
                               FontSize="{DynamicResource AppFontSizeHint}"
                               FontFamily="Consolas, Cascadia Mono"
                               Text="{Binding LogDirectoryHint}" Margin="0,0,0,12"/>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <Button Content="Clear Mithril logs" Padding="10,4" Margin="0,0,8,0"
                                VerticalAlignment="Center"
                                ToolTip="Delete the rolling diagnostics logs (mithril-*.json). Files in use stay until restart."
                                Command="{Binding ClearMithrilLogsCommand}"/>
                        <Button Content="Clear all logs" Padding="10,4" Margin="0,0,8,0"
                                VerticalAlignment="Center"
                                ToolTip="Delete all logs (diagnostics, boot, crash) and saved perf-trace sessions. Files in use stay until restart."
                                Command="{Binding ClearAllLogsCommand}"/>
                        <Button Content="Open log directory" Padding="10,4"
                                VerticalAlignment="Center"
                                ToolTip="Open the log folder in File Explorer."
                                Command="{Binding OpenLogDirectoryCommand}"/>
                    </StackPanel>

                    <TextBlock TextWrapping="Wrap" Foreground="#AAD4A847"
                               FontSize="{DynamicResource AppFontSizeHint}"
                               Text="{Binding MaintenanceStatus}"
                               Visibility="{Binding MaintenanceStatus, Converter={StaticResource NullOrEmptyToVis}}"/>
                </StackPanel>
            </Border>

            <!-- Telemetry export (OTLP) sub-section. The VM is always registered
                 (ShellComposition adds fallback scrubber-graph singletons when
                 OTLP export is off), so this expander is always available — letting
                 the user opt in for the first time and see the master toggle. -->
            <views:TelemetrySettingsControl DataContext="{Binding Telemetry}"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
