<UserControl x:Class="Palantir.Views.WorldStateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Palantir.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:WorldStateViewModel}"
             Background="Transparent">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Mithril.Shared.Wpf;component/Resources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="4,12,4,4">
        <StackPanel MaxWidth="640" HorizontalAlignment="Left">

            <DockPanel Margin="0,0,0,4">
                <Button DockPanel.Dock="Right" Content="Refresh"
                        Command="{Binding RefreshCommand}"
                        Padding="12,4" VerticalAlignment="Top"/>
                <TextBlock Text="World state"
                           FontFamily="{DynamicResource AppHeaderFontFamily}"
                           FontSize="{DynamicResource AppFontSizeLarge}"
                           FontWeight="SemiBold"
                           Foreground="#FFD4A847"/>
            </DockPanel>

            <TextBlock TextWrapping="Wrap" Foreground="#AAFFFFFF"
                       FontSize="{DynamicResource AppFontSizeHint}"
                       Margin="0,0,0,16">
                Live Arda world-state projection: the current area resolved
                through areas.json, the player's last-known position from
                Player.log ProcessNewPosition, and the area-scoped map-pin
                set. Position is sparse — it only updates on teleport /
                zone-in, so the measured instant can lag wall-clock by a long
                way. Pins are push-driven and area-local: PG idempotently
                replays the whole set on every login / zone change. The lunar
                phase comes from Player.log ProcessSetCelestialInfo (login +
                every phase roll-over) and backs the moon-gated recipes /
                quests. Weather is per-map (ProcessSetWeather) — it is dropped
                on a map change and reads "unknown" until the new map reports,
                so a foggy/sunny value never bleeds across a zone (the
                Vampirism-relevant case).
            </TextBlock>

            <!-- Map / area -->
            <TextBlock Text="MAP" Foreground="#88FFFFFF"
                       FontSize="{DynamicResource AppFontSizeHint}"
                       FontWeight="SemiBold" Margin="0,0,0,4"/>
            <Border Background="#FF252525" CornerRadius="4" Padding="14,10" Margin="0,0,0,16">
                <StackPanel>
                    <DockPanel Margin="0,0,0,6">
                        <TextBlock DockPanel.Dock="Left" Text="Area"
                                   Foreground="#CCFFFFFF" VerticalAlignment="Center"/>
                        <TextBlock HorizontalAlignment="Right"
                                   Text="{Binding AreaFriendlyName}"
                                   Foreground="#FFFFFFFF" FontWeight="SemiBold"/>
                    </DockPanel>
                    <DockPanel Margin="0,0,0,6"
                               Visibility="{Binding AreaShortName, Converter={StaticResource NullOrEmptyToVis}}">
                        <TextBlock DockPanel.Dock="Left" Text="Short name"
                                   Foreground="#CCFFFFFF" VerticalAlignment="Center"/>
                        <TextBlock HorizontalAlignment="Right"
                                   Text="{Binding AreaShortName}"
                                   Foreground="#CCFFFFFF"/>
                    </DockPanel>
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Left" Text="Area key"
                                   Foreground="#88FFFFFF" VerticalAlignment="Center"/>
                        <TextBlock HorizontalAlignment="Right"
                                   Text="{Binding AreaKey}"
                                   FontFamily="{DynamicResource AppMonoFontFamily}"
                                   Foreground="#88FFFFFF"/>
                    </DockPanel>
                </StackPanel>
            </Border>

            <!-- Position -->
            <TextBlock Text="POSITION" Foreground="#88FFFFFF"
                       FontSize="{DynamicResource AppFontSizeHint}"
                       FontWeight="SemiBold" Margin="0,0,0,4"/>
            <Border Background="#FF252525" CornerRadius="4" Padding="14,10">
                <StackPanel>
                    <DockPanel Margin="0,0,0,6">
                        <TextBlock DockPanel.Dock="Left" Text="Coordinates"
                                   Foreground="#CCFFFFFF" VerticalAlignment="Center"/>
                        <TextBlock HorizontalAlignment="Right"
                                   Text="{Binding PositionText}"
                                   FontFamily="{DynamicResource AppMonoFontFamily}"
                                   Foreground="#FFFFFFFF"/>
                    </DockPanel>
                    <DockPanel Margin="0,0,0,6">
                        <TextBlock DockPanel.Dock="Left" Text="Measured (UTC)"
                                   Foreground="#88FFFFFF" VerticalAlignment="Center"/>
                        <TextBlock HorizontalAlignment="Right"
                                   Text="{Binding MeasuredAtText}"
                                   FontFamily="{DynamicResource AppMonoFontFamily}"
                                   Foreground="#88FFFFFF"/>
                    </DockPanel>
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Left" Text="Source"
                                   Foreground="#88FFFFFF" VerticalAlignment="Center"/>
                        <TextBlock HorizontalAlignment="Right"
                                   Text="{Binding PositionSourceText}"
                                   Foreground="#88FFFFFF"/>
                    </DockPanel>
                </StackPanel>
            </Border>

            <!-- Pins -->
            <TextBlock Text="PINS" Foreground="#88FFFFFF"
                       FontSize="{DynamicResource AppFontSizeHint}"
                       FontWeight="SemiBold" Margin="0,16,0,4"/>
            <Border Background="#FF252525" CornerRadius="4" Padding="14,10">
                <StackPanel>
                    <DockPanel Margin="0,0,0,6">
                        <TextBlock DockPanel.Dock="Left" Text="Pins (this area)"
                                   Foreground="#CCFFFFFF" VerticalAlignment="Center"/>
                        <TextBlock HorizontalAlignment="Right"
                                   Text="{Binding PinCount, StringFormat={}{0} pin(s)}"
                                   Foreground="#FFFFFFFF" FontWeight="SemiBold"/>
                    </DockPanel>
                    <DockPanel Margin="0,0,0,6">
                        <TextBlock DockPanel.Dock="Left" Text="Last change (UTC)"
                                   Foreground="#88FFFFFF" VerticalAlignment="Center"/>
                        <TextBlock HorizontalAlignment="Right"
                                   Text="{Binding PinsObservedAtText}"
                                   FontFamily="{DynamicResource AppMonoFontFamily}"
                                   Foreground="#88FFFFFF"/>
                    </DockPanel>

                    <TextBlock Text="(no pins in this area)"
                               Foreground="#66FFFFFF" FontStyle="Italic"
                               Margin="0,4,0,0"
                               Visibility="{Binding HasPins, Converter={StaticResource InverseBoolToVis}}"/>

                    <ItemsControl ItemsSource="{Binding Pins}"
                                  Margin="0,4,0,0"
                                  Visibility="{Binding HasPins, Converter={StaticResource BoolToVis}}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="0,2">
                                    <TextBlock Text="{Binding Label, Converter={StaticResource EmptyStringFallback}, ConverterParameter='Unnamed pin'}"
                                               FontWeight="SemiBold" Margin="0,0,8,0"/>
                                    <TextBlock>
                                        <Run Text="X "/>
                                        <Run Text="{Binding X, StringFormat={}{0:0.00}, Mode=OneWay}"/>
                                        <Run Text="   Z "/>
                                        <Run Text="{Binding Z, StringFormat={}{0:0.00}, Mode=OneWay}"/>
                                    </TextBlock>
                                    <TextBlock Margin="8,0,0,0" Opacity="0.6">
                                        <Run Text="Color "/>
                                        <Run Text="{Binding Color, Mode=OneWay}"/>
                                        <Run Text=" · Shape "/>
                                        <Run Text="{Binding Shape, Mode=OneWay}"/>
                                    </TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <!-- Moon / celestial -->
            <TextBlock Text="MOON" Foreground="#88FFFFFF"
                       FontSize="{DynamicResource AppFontSizeHint}"
                       FontWeight="SemiBold" Margin="0,16,0,4"/>
            <Border Background="#FF252525" CornerRadius="4" Padding="14,10">
                <StackPanel>
                    <DockPanel Margin="0,0,0,6">
                        <TextBlock DockPanel.Dock="Left" Text="Phase"
                                   Foreground="#CCFFFFFF" VerticalAlignment="Center"/>
                        <TextBlock HorizontalAlignment="Right"
                                   Text="{Binding MoonPhaseText}"
                                   Foreground="#FFFFFFFF" FontWeight="SemiBold"/>
                    </DockPanel>
                    <DockPanel Margin="0,0,0,6">
                        <TextBlock DockPanel.Dock="Left" Text="Raw token"
                                   Foreground="#88FFFFFF" VerticalAlignment="Center"/>
                        <TextBlock HorizontalAlignment="Right"
                                   Text="{Binding MoonPhaseRawText}"
                                   FontFamily="{DynamicResource AppMonoFontFamily}"
                                   Foreground="#88FFFFFF"/>
                    </DockPanel>
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Left" Text="Measured (UTC)"
                                   Foreground="#88FFFFFF" VerticalAlignment="Center"/>
                        <TextBlock HorizontalAlignment="Right"
                                   Text="{Binding MoonMeasuredAtText}"
                                   FontFamily="{DynamicResource AppMonoFontFamily}"
                                   Foreground="#88FFFFFF"/>
                    </DockPanel>
                </StackPanel>
            </Border>

            <!-- Weather -->
            <TextBlock Text="WEATHER" Foreground="#88FFFFFF"
                       FontSize="{DynamicResource AppFontSizeHint}"
                       FontWeight="SemiBold" Margin="0,16,0,4"/>
            <Border Background="#FF252525" CornerRadius="4" Padding="14,10">
                <StackPanel>
                    <DockPanel Margin="0,0,0,6">
                        <TextBlock DockPanel.Dock="Left" Text="Condition (this map)"
                                   Foreground="#CCFFFFFF" VerticalAlignment="Center"/>
                        <TextBlock HorizontalAlignment="Right"
                                   Text="{Binding WeatherConditionText}"
                                   Foreground="#FFFFFFFF" FontWeight="SemiBold"/>
                    </DockPanel>
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Left" Text="Last change (UTC)"
                                   Foreground="#88FFFFFF" VerticalAlignment="Center"/>
                        <TextBlock HorizontalAlignment="Right"
                                   Text="{Binding WeatherObservedAtText}"
                                   FontFamily="{DynamicResource AppMonoFontFamily}"
                                   Foreground="#88FFFFFF"/>
                    </DockPanel>

                    <TextBlock Text="(weather unknown — left the map, or not yet reported)"
                               Foreground="#66FFFFFF" FontStyle="Italic"
                               Margin="0,4,0,0"
                               Visibility="{Binding HasWeather, Converter={StaticResource InverseBoolToVis}}"/>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
