<UserControl x:Class="Legolas.Views.WizardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:vm="clr-namespace:Legolas.ViewModels"
             xmlns:controls="clr-namespace:Legolas.Controls"
             Background="{DynamicResource BgPrimaryBrush}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Mithril.Shared.Wpf;component/Resources.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- Primary action button. BasedOn the implicit Button style is *required* —
                 keyed styles don't inherit the implicit one automatically, and skipping it
                 reverts the button to WPF's default chrome (light gray box, no Mithril theme). -->
            <Style x:Key="WizardPrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Padding" Value="18,8" />
                <Setter Property="MinWidth" Value="140" />
                <Setter Property="Margin" Value="0,0,12,0" />
            </Style>

            <!-- Breadcrumb step label. Highlight applied per-step via DataTrigger inline. -->
            <Style x:Key="WizardBreadcrumbStep" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{StaticResource TextTertiaryBrush}" />
                <Setter Property="FontSize" Value="{DynamicResource AppFontSize}" />
                <Setter Property="VerticalAlignment" Value="Center" />
            </Style>
            <Style x:Key="WizardBreadcrumbSeparator" TargetType="TextBlock" BasedOn="{StaticResource WizardBreadcrumbStep}">
                <Setter Property="Text" Value="›" />
                <Setter Property="Margin" Value="10,0" />
            </Style>

            <!-- #476 Option C: shared "set my position" affordance, hosted in
                 both the Listening and Gathering panels (the two phases the
                 SurveyFlow SettingPosition detour returns to). DataContext is
                 the wizard VM, so MapOverlay.* binds through. -->
            <DataTemplate x:Key="SetPositionAffordance">
                <Grid>
                    <Button Content="Set my position…"
                            HorizontalAlignment="Center"
                            Command="{Binding MapOverlay.SetPositionCommand}"
                            Visibility="{Binding MapOverlay.IsSettingPosition, Converter={x:Static controls:BoolToVisibilityConverter.Inverse}}"
                            ToolTip="Override the auto position marker — click the map where you're standing. Use this if the marker looks stale or wrong." />
                    <Border Visibility="{Binding MapOverlay.IsSettingPosition, Converter={x:Static controls:BoolToVisibilityConverter.Instance}}"
                            Background="{StaticResource AccentSoftBrush}" CornerRadius="3" Padding="12,8">
                        <StackPanel>
                            <TextBlock Text="Click the map where your character is standing now."
                                       TextAlignment="Center" TextWrapping="Wrap" MaxWidth="320"
                                       Foreground="{StaticResource AccentSoftTextBrush}"
                                       FontSize="{DynamicResource AppFontSizeBody}" Margin="0,0,0,8" />
                            <Button Content="Cancel" HorizontalAlignment="Center"
                                    Command="{Binding MapOverlay.CancelSetPositionCommand}" />
                        </StackPanel>
                    </Border>
                </Grid>
            </DataTemplate>

            <!-- #113 Layer 2: plain-language fix-confidence palette. Muted to
                 sit in the Mithril theme; semantics carried by the text too,
                 not colour alone. -->
            <SolidColorBrush x:Key="MlQualityStrongBrush" Color="#3E8E5A" />
            <SolidColorBrush x:Key="MlQualityUsableBrush" Color="#C99A2E" />
            <SolidColorBrush x:Key="MlQualityRoughBrush"  Color="#C2702F" />

            <!-- #113 Layers 1–3: one located/locating treasure row — route
                 badge, relative-location headline (raw coord/GDOP/residual
                 demoted to the tooltip), confidence pill, collected state. -->
            <DataTemplate x:Key="MotherlodeSlotRow" DataType="{x:Type vm:MotherlodeSlotViewModel}">
                <Border CornerRadius="4" Padding="8,5" Margin="0,2" Background="Transparent">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsNextUp}" Value="True">
                                    <Setter Property="Background" Value="{StaticResource AccentSoftBrush}" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="28" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="{Binding RouteText}" TextAlignment="Right"
                                   Margin="0,0,10,0" VerticalAlignment="Center"
                                   Foreground="{StaticResource TextTertiaryBrush}"
                                   FontSize="{DynamicResource AppFontSize}" />

                        <TextBlock Grid.Column="1" Text="{Binding HeadlineText}" VerticalAlignment="Center"
                                   TextWrapping="Wrap" ToolTip="{Binding DetailText}"
                                   FontSize="{DynamicResource AppFontSizeBody}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasFix}" Value="False">
                                            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
                                            <Setter Property="FontStyle" Value="Italic" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Collected}" Value="True">
                                            <Setter Property="TextDecorations" Value="Strikethrough" />
                                            <Setter Property="Foreground" Value="{StaticResource TextTertiaryBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <!-- Confidence pill (#113 Layer 2). Hidden until solved. -->
                        <Border Grid.Column="2" CornerRadius="9" Padding="8,2" Margin="10,0,0,0"
                                VerticalAlignment="Center">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding QualityText}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Quality}" Value="Strong">
                                            <Setter Property="Background" Value="{StaticResource MlQualityStrongBrush}" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Quality}" Value="Usable">
                                            <Setter Property="Background" Value="{StaticResource MlQualityUsableBrush}" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Quality}" Value="Rough">
                                            <Setter Property="Background" Value="{StaticResource MlQualityRoughBrush}" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Collected}" Value="True">
                                            <Setter Property="Opacity" Value="0.4" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="{Binding QualityText}" Foreground="White"
                                       FontSize="{DynamicResource AppFontSizeSmall}" FontWeight="SemiBold" />
                        </Border>

                        <icon:PackIconLucide Grid.Column="3" Kind="Check" Width="14" Height="14"
                                             Margin="10,0,0,0" VerticalAlignment="Center"
                                             Foreground="{StaticResource AccentBrush}"
                                             Visibility="{Binding Collected, Converter={x:Static controls:BoolToVisibilityConverter.Instance}}" />
                    </Grid>
                </Border>
            </DataTemplate>

            <!-- Shared Locating/Walk body (DataContext = MotherlodeViewModel):
                 counters, guidance callout, the located/locating list. The
                 step-specific prompt + buttons live in each panel. -->
            <DataTemplate x:Key="MotherlodeProgressBody">
                <StackPanel HorizontalAlignment="Center" MinWidth="360">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6" HorizontalAlignment="Center">
                        <TextBlock Text="Locations measured: " Foreground="{StaticResource TextTertiaryBrush}"
                                   FontSize="{DynamicResource AppFontSizeBody}" VerticalAlignment="Center" />
                        <TextBlock Text="{Binding LocationCount}" Margin="4,0,0,0" FontWeight="SemiBold"
                                   FontSize="{DynamicResource AppFontSizeBody}" VerticalAlignment="Center" />
                        <TextBlock Margin="6,0,0,0" Foreground="{StaticResource TextTertiaryBrush}"
                                   FontSize="{DynamicResource AppFontSizeBody}" VerticalAlignment="Center"
                                   Text="{Binding LocationsWithFix, StringFormat=({0} with a position fix)}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12" HorizontalAlignment="Center">
                        <TextBlock Text="Treasures located: " Foreground="{StaticResource TextTertiaryBrush}"
                                   FontSize="{DynamicResource AppFontSizeBody}" VerticalAlignment="Center" />
                        <TextBlock Text="{Binding SolvedCount}" Margin="4,0,0,0" FontWeight="SemiBold"
                                   FontSize="{DynamicResource AppFontSizeBody}" VerticalAlignment="Center" />
                    </StackPanel>

                    <!-- #488 Multi-map mode. DELIBERATELY surfaced here on the
                         Motherlode panel, NOT in the Legolas settings tab:
                         it's an operational control the player flips mid-run
                         (one map vs a carried batch), so it belongs where the
                         action is. It is still a persisted setting
                         (LegolasSettings.MotherlodeMultiMapMode); the panel is
                         just its edit+display surface. Do not relocate to
                         settings. -->
                    <CheckBox IsChecked="{Binding MultiMapMode, Mode=TwoWay}"
                              HorizontalAlignment="Center" Margin="0,0,0,4"
                              Content="Multi-map mode — I read my maps in the same order at every spot" />
                    <TextBlock Text="{Binding ContractHint}" TextWrapping="Wrap"
                               HorizontalAlignment="Center" TextAlignment="Center" Margin="0,0,0,8"
                               FontSize="{DynamicResource AppFontSizeSmall}"
                               Foreground="{StaticResource TextTertiaryBrush}">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Visible" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ContractHint}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <!-- Passive per-spot read tally — the multi-map shape at a
                         glance; the actionable warning lives in the Guidance
                         border below. -->
                    <TextBlock Text="{Binding ReadsSummary}" TextWrapping="Wrap"
                               HorizontalAlignment="Center" TextAlignment="Center" Margin="0,0,0,10"
                               FontSize="{DynamicResource AppFontSizeBody}"
                               Foreground="{StaticResource TextTertiaryBrush}">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Visible" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ReadsSummary}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <!-- #506: guided next measurement spot (≥1 committed spot). -->
                    <Border Margin="0,0,0,12" Padding="10,8" CornerRadius="4"
                            Background="{StaticResource AccentSoftBrush}">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Visibility" Value="Visible" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding GuidedSpotPhrase}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <TextBlock Text="{Binding GuidedSpotPhrase}" TextWrapping="Wrap"
                                   FontSize="{DynamicResource AppFontSizeBody}"
                                   Foreground="{StaticResource TextSecondaryBrush}" />
                    </Border>

                    <Border Margin="0,0,0,12" Padding="10,8" CornerRadius="4"
                            Background="{StaticResource AccentSoftBrush}">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Visibility" Value="Visible" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Guidance}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <TextBlock Text="{Binding Guidance}" TextWrapping="Wrap"
                                   FontSize="{DynamicResource AppFontSizeBody}"
                                   Foreground="{StaticResource TextSecondaryBrush}" />
                    </Border>

                    <ItemsControl ItemsSource="{Binding Slots}"
                                  ItemTemplate="{StaticResource MotherlodeSlotRow}"
                                  Margin="0,0,0,12" />
                </StackPanel>
            </DataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>

    <DockPanel LastChildFill="True">

        <!-- Module header (Mithril convention: every module's view starts with
             icon + accented title at the top). #113: a persistent area +
             calibration-state chip on the right — the user-facing way to see
             calibration state (the calibration overlay is experimental). When
             the area is known but uncalibrated the chip is clickable and
             starts the guided Drop/Pair calibration. #524: the map-zoom
             strip docks alongside; it's always reachable here (this panel is
             a normal opaque window, never click-through). -->
        <DockPanel DockPanel.Dock="Top" Margin="20,16,20,0">
            <!-- #501: the chip is the single calibrate/recalibrate entry
                 point. Uncalibrated → guided cold-start; calibrated → arms the
                 #477B confirm guard surfaced as the popup below (anchored
                 here), so one click can't wipe a good calibration. -->
            <Border x:Name="CalibrationChip"
                    DockPanel.Dock="Right" VerticalAlignment="Center"
                    Background="{StaticResource BgSurfaceBrush}"
                    BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1"
                    CornerRadius="10" Padding="10,4"
                    ToolTip="The area Legolas detected and whether it's calibrated. Calibration is only needed for the on-map dot — the relative directions are exact without it. Click to calibrate when uncalibrated, or to redo the calibration when it's already set.">
                <Border.InputBindings>
                    <MouseBinding Gesture="LeftClick" Command="{Binding CalibrateThisAreaCommand}" />
                </Border.InputBindings>
                <Border.Style>
                    <Style TargetType="Border">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding CanCalibrateThisArea}" Value="True">
                                <Setter Property="Cursor" Value="Hand" />
                                <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel Orientation="Horizontal">
                    <Ellipse Width="8" Height="8" VerticalAlignment="Center" Margin="0,0,7,0">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <!-- area unknown → grey -->
                                <Setter Property="Fill" Value="{StaticResource TextTertiaryBrush}" />
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding IsAreaKnown}" Value="True" />
                                            <Condition Binding="{Binding IsAreaCalibrated}" Value="True" />
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Fill" Value="{StaticResource MlQualityStrongBrush}" />
                                    </MultiDataTrigger>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding IsAreaKnown}" Value="True" />
                                            <Condition Binding="{Binding IsAreaCalibrated}" Value="False" />
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Fill" Value="{StaticResource MlQualityUsableBrush}" />
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock Text="{Binding CalibrationChipText}" VerticalAlignment="Center"
                               FontSize="{DynamicResource AppFontSizeSmall}"
                               Foreground="{StaticResource TextSecondaryBrush}" />

                    <!-- #503: the "Validate calibration" toggle, folded into
                         the chip as a split segment (it dwarfed the chip as a
                         standalone labelled button). The dot+text region stays
                         the calibrate/recalibrate target via the Border
                         MouseBinding; this trailing icon is its own Button so
                         its click is handled and never bubbles to that binding.
                         The whole segment is hidden when there's nothing to
                         validate (uncalibrated). Disabled while surveying /
                         plotting motherlodes (CanValidateCalibration), where
                         the markers would clutter the working overlay. -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center"
                                Visibility="{Binding IsAreaCalibrated, Converter={x:Static controls:BoolToVisibilityConverter.Instance}}">
                        <Rectangle Width="1" Margin="9,2,8,2"
                                   Fill="{StaticResource BorderSubtleBrush}" />
                        <Button Command="{Binding MapOverlay.ToggleCalibrationValidationCommand}"
                                IsEnabled="{Binding CanValidateCalibration}"
                                VerticalAlignment="Center" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="Transparent" Padding="2">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center" />
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="ToolTip" Value="Show known landmarks/NPCs on the map to check the calibration still lines up — a consistent offset means recalibrate. Tip: after a PG client restart, leave Mithril's zoom slider at the calibration stamp and drag PG's zoom slider until the ghosts align with their real landmarks — once they do, PG matches Mithril without typing numbers. Available between runs, not while surveying or plotting motherlodes." />
                                    <Setter Property="Opacity" Value="1" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding MapOverlay.ShowCalibrationGhosts}" Value="True">
                                            <Setter Property="ToolTip" Value="Hide the reference markers." />
                                        </DataTrigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter Property="Opacity" Value="0.4" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <icon:PackIconLucide Width="13" Height="13" Kind="Crosshair">
                                <icon:PackIconLucide.Style>
                                    <Style TargetType="icon:PackIconLucide">
                                        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding MapOverlay.ShowCalibrationGhosts}" Value="True">
                                                <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </icon:PackIconLucide.Style>
                            </icon:PackIconLucide>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Border>

            <StackPanel Orientation="Horizontal">
                <Image Source="pack://application:,,,/Legolas.Module;component/Resources/legolas.ico"
                       Width="28" Height="28" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Text="Legolas · Survey"
                           FontFamily="{DynamicResource AppHeaderFontFamily}" FontSize="{DynamicResource AppFontSizeHero}" FontWeight="SemiBold"
                           Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center"/>
            </StackPanel>
        </DockPanel>

        <!-- Footer (always visible): status strip -->
        <Border DockPanel.Dock="Bottom" Background="{StaticResource BgSurfaceBrush}"
                BorderBrush="{StaticResource TextTertiaryBrush}" BorderThickness="0,1,0,0"
                Padding="20,10">
            <TextBlock Foreground="{StaticResource TextTertiaryBrush}"
                       Text="{Binding SurveyFlow.PhaseDescription}"
                       TextTrimming="CharacterEllipsis" />
        </Border>

        <!-- Breadcrumb (visible after the user has picked a mode) -->
        <Border DockPanel.Dock="Top" Margin="20,16,20,0"
                Visibility="{Binding HasPickedMode, Converter={x:Static controls:BoolToVisibilityConverter.Instance}}">
            <Grid HorizontalAlignment="Center">
                <!-- Survey breadcrumb -->
                <StackPanel Orientation="Horizontal"
                            Visibility="{Binding Session.Mode, Converter={x:Static controls:EnumMatchConverter.ToVisibility}, ConverterParameter=Survey}">
                    <TextBlock Text="Mode" Style="{StaticResource WizardBreadcrumbStep}" />
                    <TextBlock Style="{StaticResource WizardBreadcrumbSeparator}" />
                    <!-- #460: cold-start calibration gate (skipped when the
                         area is already calibrated). -->
                    <TextBlock Text="Calibrate">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource WizardBreadcrumbStep}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CurrentStep}" Value="Calibrating">
                                        <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                                        <Setter Property="FontWeight" Value="SemiBold" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <TextBlock Style="{StaticResource WizardBreadcrumbSeparator}" />
                    <TextBlock Text="Listen">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource WizardBreadcrumbStep}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CurrentStep}" Value="Listening">
                                        <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                                        <Setter Property="FontWeight" Value="SemiBold" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <TextBlock Style="{StaticResource WizardBreadcrumbSeparator}" />
                    <TextBlock Text="Walk">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource WizardBreadcrumbStep}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CurrentStep}" Value="Gathering">
                                        <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                                        <Setter Property="FontWeight" Value="SemiBold" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <TextBlock Style="{StaticResource WizardBreadcrumbSeparator}" />
                    <TextBlock Text="Done">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource WizardBreadcrumbStep}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CurrentStep}" Value="Done">
                                        <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                                        <Setter Property="FontWeight" Value="SemiBold" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
                <!-- Motherlode breadcrumb (#113 Layer 4: derived sub-steps) -->
                <StackPanel Orientation="Horizontal"
                            Visibility="{Binding Session.Mode, Converter={x:Static controls:EnumMatchConverter.ToVisibility}, ConverterParameter=Motherlode}">
                    <TextBlock Text="Mode" Style="{StaticResource WizardBreadcrumbStep}" />
                    <TextBlock Style="{StaticResource WizardBreadcrumbSeparator}" />
                    <TextBlock Text="Measure">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource WizardBreadcrumbStep}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CurrentStep}" Value="MotherlodeMeasuring">
                                        <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                                        <Setter Property="FontWeight" Value="SemiBold" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <TextBlock Style="{StaticResource WizardBreadcrumbSeparator}" />
                    <TextBlock Text="Locate">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource WizardBreadcrumbStep}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CurrentStep}" Value="MotherlodeLocating">
                                        <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                                        <Setter Property="FontWeight" Value="SemiBold" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <TextBlock Style="{StaticResource WizardBreadcrumbSeparator}" />
                    <TextBlock Text="Walk">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource WizardBreadcrumbStep}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CurrentStep}" Value="MotherlodeWalk">
                                        <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                                        <Setter Property="FontWeight" Value="SemiBold" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <TextBlock Style="{StaticResource WizardBreadcrumbSeparator}" />
                    <TextBlock Text="Done">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource WizardBreadcrumbStep}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CurrentStep}" Value="MotherlodeDone">
                                        <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                                        <Setter Property="FontWeight" Value="SemiBold" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Step body: each step's panel centers itself; the visible one fills the available area.
             A shared hero row at the top of every step renders [← back] [title] [↻ reset] together. -->
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalContentAlignment="Center">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top"
                        MaxWidth="780" Margin="20,24,20,16">

                <!-- Shared step hero row: nav + title.
                     Back/reset hidden in PickMode (no flow yet). Title spans by binding to CurrentStepTitle. -->
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="40" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Style="{StaticResource MithrilToolbarButtonStyle}"
                            Command="{Binding BackCommand}" ToolTip="Back one step"
                            VerticalAlignment="Center" HorizontalAlignment="Left"
                            Visibility="{Binding HasPickedMode, Converter={x:Static controls:BoolToVisibilityConverter.Instance}}">
                        <icon:PackIconLucide Kind="ArrowLeft" Width="14" Height="14" />
                    </Button>
                    <TextBlock Grid.Column="1" Text="{Binding CurrentStepTitle}"
                               FontSize="32" FontWeight="SemiBold"
                               HorizontalAlignment="Center" TextAlignment="Center"
                               Foreground="{StaticResource TextPrimaryBrush}" />
                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- #113: the standalone calibration overlay is
                             experimental, not for general use — calibration is
                             reached via the guided flow (the header chip /
                             Survey cold-start / Motherlode opt-in). Kept here
                             but collapsed rather than deleted so the binding +
                             hotkey path stay intact for experimental use. -->
                        <Button Style="{StaticResource MithrilToolbarButtonStyle}"
                                Command="{Binding ToggleCalibrationCommand}"
                                ToolTip="Open the map-calibration overlay (click landmarks/NPCs to fix this area's projection)"
                                Margin="0,0,4,0"
                                Visibility="Collapsed">
                            <icon:PackIconLucide Kind="Crosshair" Width="14" Height="14" />
                        </Button>
                        <!-- Overlay/reset are flow controls — only after a mode is picked. -->
                        <StackPanel Orientation="Horizontal"
                                    Visibility="{Binding HasPickedMode, Converter={x:Static controls:BoolToVisibilityConverter.Instance}}">
                            <Button Style="{StaticResource MithrilToolbarButtonStyle}"
                                    Command="{Binding ToggleOverlaysCommand}"
                                    ToolTip="Show or hide both overlays (map and inventory)"
                                    Margin="0,0,4,0">
                                <icon:PackIconLucide Kind="Eye" Width="14" Height="14" />
                            </Button>
                            <Button Style="{StaticResource MithrilToolbarButtonStyle}"
                                    Command="{Binding WizardResetCommand}" ToolTip="Reset this flow (overlays stay open)">
                                <icon:PackIconLucide Kind="RotateCcw" Width="14" Height="14" />
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </Grid>

                <!-- Step bodies — one visible at a time, keyed off CurrentStep. -->

                <!-- PickMode -->
                <StackPanel Visibility="{Binding CurrentStep, Converter={x:Static controls:EnumMatchConverter.ToVisibility}, ConverterParameter=PickMode}">
                    <TextBlock TextWrapping="Wrap" HorizontalAlignment="Center" TextAlignment="Center" Margin="0,0,0,32"
                               FontSize="{DynamicResource AppFontSizeBody}"
                               Foreground="{StaticResource TextSecondaryBrush}">
                        <Run Text="Two flows: " />
                        <Run Text="Survey" FontWeight="SemiBold" />
                        <Run Text=" — using survey items to find slabs / minerals — or " />
                        <Run Text="Motherlode" FontWeight="SemiBold" />
                        <Run Text=" — trilaterating buried treasure from a Treasure Map." />
                    </TextBlock>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="Survey" Style="{StaticResource WizardPrimaryButton}"
                                Command="{Binding PickSurveyModeCommand}" />
                        <Button Content="Motherlode" Style="{StaticResource WizardPrimaryButton}" Margin="0"
                                Command="{Binding PickMotherlodeModeCommand}" />
                    </StackPanel>
                </StackPanel>

                <!-- Calibrating (#460/#477A: guided two-phase correctable
                     walkthrough. Capture happens on the map overlay, not a
                     separate window; this always-clickable panel hosts the
                     phase trigger, the named-pin prompt, the live residual,
                     and the terminal Confirm. -->
                <StackPanel Visibility="{Binding CurrentStep, Converter={x:Static controls:EnumMatchConverter.ToVisibility}, ConverterParameter=Calibrating}">

                    <!-- #501 (reworked): recalibrate confirm gate, inline at
                         the top of the Calibrating step (replaces the flaky
                         chip popup). Shown only while IsConfirmingRecalibrate;
                         the drop/pair body below is hidden until acknowledged.
                         Confirm does NOT delete — the existing calibration
                         stays live until the new fit is solved, so a bailed
                         recalibration loses nothing. Cancel exits with the old
                         calibration intact. -->
                    <Border Visibility="{Binding IsConfirmingRecalibrate, Converter={x:Static controls:BoolToVisibilityConverter.Instance}}"
                            Background="{StaticResource AccentSoftBrush}"
                            BorderBrush="{StaticResource AccentBrush}" BorderThickness="1"
                            CornerRadius="4" Padding="16,12" Margin="0,0,0,8"
                            HorizontalAlignment="Center" MaxWidth="420">
                        <StackPanel>
                            <TextBlock Text="Replace this area's saved calibration?"
                                       TextAlignment="Center" FontWeight="SemiBold"
                                       Foreground="{StaticResource AccentSoftTextBrush}"
                                       FontSize="{DynamicResource AppFontSizeBody}" Margin="0,0,0,4" />
                            <TextBlock Text="The current calibration stays active until you finish and save a new one — cancelling here keeps it unchanged."
                                       TextAlignment="Center" TextWrapping="Wrap"
                                       Foreground="{StaticResource AccentSoftTextBrush}"
                                       FontSize="{DynamicResource AppFontSizeBody}" Margin="0,0,0,10" />
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="Replace calibration" Margin="0,0,8,0"
                                        Command="{Binding ConfirmRecalibrateCommand}" />
                                <Button Content="Cancel"
                                        Command="{Binding CancelRecalibrateCommand}" />
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Drop/pair body — hidden behind the confirm gate above
                         when a recalibration is awaiting acknowledgement. -->
                    <StackPanel Visibility="{Binding IsConfirmingRecalibrate, Converter={x:Static controls:BoolToVisibilityConverter.Inverse}}">
                    <TextBlock TextWrapping="Wrap" HorizontalAlignment="Center" TextAlignment="Center" Margin="0,0,0,12"
                               FontSize="{DynamicResource AppFontSizeBody}"
                               Foreground="{StaticResource TextSecondaryBrush}">
                        <Run Text="Pins place against this area's calibration. Two steps: drop well-spread map pins in-game, then click each one through the overlay to pair it." />
                    </TextBlock>

                    <!-- Live status (drop count / named-pin prompt). -->
                    <TextBlock Text="{Binding PinCalibration.StatusText}"
                               TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,12"
                               FontSize="{DynamicResource AppFontSizeBody}"
                               Foreground="{StaticResource AccentBrush}" />

                    <!-- Phase trigger: "Done dropping — pair them" / "Drop more
                         pins". Always clickable (this panel is not the
                         transparent overlay). An optional hotkey mirrors it. -->
                    <Button Content="{Binding PinCalibration.PhaseToggleLabel}"
                            Style="{StaticResource WizardPrimaryButton}"
                            HorizontalAlignment="Center" Margin="0,0,0,12"
                            Command="{Binding ToggleCalibrationPhaseCommand}" />

                    <!-- Pair-phase controls: skip the named pin, or override
                         to any pin by identity (UX only — solve stays
                         world↔pixel). -->
                    <StackPanel Visibility="{Binding PinCalibration.IsPairing, Converter={x:Static controls:BoolToVisibilityConverter.Instance}}"
                                Margin="0,0,0,12">
                        <Button Content="Skip this pin" Style="{StaticResource WizardPrimaryButton}"
                                HorizontalAlignment="Center" Margin="0,0,0,8"
                                Command="{Binding SkipCalibrationPinCommand}" />
                        <TextBlock Text="Or pick the pin you're clicking (optional):"
                                   TextAlignment="Center"
                                   FontSize="{DynamicResource AppFontSize}"
                                   Foreground="{StaticResource TextTertiaryBrush}" Margin="0,0,0,4" />
                        <ListBox ItemsSource="{Binding PinCalibration.ExistingPins}"
                                 SelectedItem="{Binding PinCalibration.OverridePin, Mode=TwoWay}"
                                 MaxHeight="140" HorizontalAlignment="Center" MinWidth="240"
                                 Background="Transparent" BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock FontSize="{DynamicResource AppFontSizeBody}"
                                               Foreground="{StaticResource TextPrimaryBrush}">
                                        <Run Text="{Binding DisplayName, Mode=OneWay}" FontWeight="SemiBold" />
                                        <Run Text="{Binding Appearance, Mode=OneWay, StringFormat=({0})}"
                                             Foreground="{StaticResource TextSecondaryBrush}" />
                                    </TextBlock>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>

                    <!-- Live, non-persisting fit quality (≥3 pairs). -->
                    <TextBlock Text="{Binding PinCalibration.ResidualText}"
                               TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,12"
                               FontSize="{DynamicResource AppFontSizeBody}"
                               Foreground="{StaticResource TextSecondaryBrush}" />

                    <!-- #1095: zoom strip removed. The live-view detector
                         reads the actual pan + scale from PG's map region;
                         the user no longer needs to dial a zoom value. -->
                    <TextBlock Text="Zoom out fully (entire map visible), then click the first landmark."
                               HorizontalAlignment="Center" Margin="0,0,0,12"
                               VerticalAlignment="Center"
                               FontStyle="Italic"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               FontSize="{DynamicResource AppFontSizeBody}" />

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="Confirm" Style="{StaticResource WizardPrimaryButton}"
                                Command="{Binding ConfirmCalibrationCommand}"
                                ToolTip="Save this calibration and continue"
                                IsEnabled="{Binding PinCalibration.IsResidualGood}" />
                        <!-- finish anyway: only when ≥3 pairs but the residual
                             is above target (never trap the user at the ±10%
                             ceiling). -->
                        <Button Content="Finish anyway"
                                Command="{Binding ConfirmCalibrationAnywayCommand}"
                                ToolTip="The fit is loose (the in-game map isn't perfectly affine) — save it regardless"
                                IsEnabled="{Binding PinCalibration.CanConfirm}">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource WizardPrimaryButton}">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding PinCalibration.CanConfirm}" Value="True" />
                                                <Condition Binding="{Binding PinCalibration.IsResidualGood}" Value="False" />
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Visibility" Value="Visible" />
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Content="Clear pins" Style="{StaticResource WizardPrimaryButton}" Margin="0"
                                Command="{Binding ClearCalibrationPinsCommand}" />
                    </StackPanel>
                    </StackPanel>
                </StackPanel>

                <!-- Listening (#454: no AwaitingPosition step — placement is
                     absolute via ProcessMapFx; no anchor click) -->
                <StackPanel Visibility="{Binding CurrentStep, Converter={x:Static controls:EnumMatchConverter.ToVisibility}, ConverterParameter=Listening}">

                    <!-- #524: zoom-mismatch warning. Live in-game zoom diverges
                         from the captured calibration stamp by more than half
                         a slider tick. No dismiss — clears automatically as
                         the user matches the zoom or recalibrates. Suppressed
                         for legacy 1.0 stamps (the recalibrate hint below
                         handles those). -->
                    <!-- #1095: zoom-mismatch banner removed — the new layer-2 model
                         measures PG's live view via the screenshot probe; there's no
                         "slider drifted from cal zoom" failure mode to surface anymore. -->


                    <!-- #524: legacy-stamp recalibrate hint. One-time per area
                         per session (Got it dismisses for the rest of the
                         session; drops on recalibrate). Not surfaced as a
                         warning — the calibration still works at the stamp's
                         zoom; recalibrating just unlocks cross-zoom placement. -->
                    <Border Margin="0,0,0,12" Padding="12,8" CornerRadius="3"
                            Background="{StaticResource BgSurfaceBrush}"
                            BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1"
                            HorizontalAlignment="Center" MaxWidth="520"
                            Visibility="{Binding MapOverlay.IsLegacyRecalibrateHintVisible, Converter={x:Static controls:BoolToVisibilityConverter.Instance}}">
                        <StackPanel>
                            <TextBlock Text="This area's calibration predates zoom tracking — recalibrate to enable cross-zoom placement."
                                       TextWrapping="Wrap" TextAlignment="Center"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       FontSize="{DynamicResource AppFontSizeBody}" Margin="0,0,0,6" />
                            <Button Content="Got it" HorizontalAlignment="Center"
                                    Padding="10,3"
                                    Command="{Binding MapOverlay.DismissLegacyRecalibrateHintCommand}" />
                        </StackPanel>
                    </Border>

                    <StackPanel HorizontalAlignment="Center" Margin="0,0,0,16">
                        <TextBlock Text="From your inventory, work through the surveys in order:"
                                   TextWrapping="Wrap" TextAlignment="Center"
                                   FontSize="{DynamicResource AppFontSizeBody}"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   Margin="0,0,0,8" />
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="1.  Use the leftmost survey"
                                       FontSize="{DynamicResource AppFontSizeBody}"
                                       Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,2" />
                            <TextBlock Text="2.  Its exact spot is read from the game log — the pin places itself on the map automatically. No clicking needed."
                                       TextWrapping="Wrap"
                                       FontSize="{DynamicResource AppFontSizeBody}"
                                       Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,2" />
                            <TextBlock Text="3.  Repeat left-to-right, then start the next row from the left"
                                       FontSize="{DynamicResource AppFontSizeBody}"
                                       Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,2" />
                        </StackPanel>
                        <!-- #501: recalibration moved to the always-visible
                             header chip (click a calibrated chip → the
                             header confirm popup). The old in-panel
                             "Recalibrate this area" affordance was removed —
                             one calibration entry point, reachable from any
                             step. -->
                        <TextBlock TextWrapping="Wrap" TextAlignment="Center" Margin="0,12,0,0"
                                   FontSize="{DynamicResource AppFontSizeBody}"
                                   Foreground="{StaticResource TextTertiaryBrush}">
                            <Run Text="Tip: fine-tune the selected pin with the Pin Nudge hotkeys — bind keys under Settings → Hotkeys, category " />
                            <Run Text="Legolas · Pin Nudge" FontStyle="Italic" />
                            <Run Text="." />
                        </TextBlock>
                    </StackPanel>
                    <!-- Survey list. Selecting an item sets Session.SelectedSurvey, which
                         the overlay drag-pad uses as its target and the nudge pad routes
                         to. ListBox uses VirtualizingStackPanel by default — important for
                         survey runs of 100+ pins. -->
                    <Border Background="{StaticResource BgSurfaceBrush}"
                            BorderBrush="{StaticResource TextTertiaryBrush}" BorderThickness="1"
                            CornerRadius="3" Margin="0,0,0,12">
                        <DockPanel>
                            <Border DockPanel.Dock="Top"
                                    Background="{StaticResource AccentSoftBrush}"
                                    Padding="10,4">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Pins placed: " Foreground="{StaticResource AccentSoftTextBrush}"
                                               FontSize="{DynamicResource AppFontSizeBody}" />
                                    <TextBlock Text="{Binding Session.Surveys.Count}" FontWeight="SemiBold"
                                               FontSize="{DynamicResource AppFontSizeBody}"
                                               Foreground="{StaticResource AccentSoftTextBrush}" />
                                    <TextBlock Text=" — click to select" Foreground="{StaticResource AccentSoftTextBrush}"
                                               FontSize="{DynamicResource AppFontSize}"
                                               VerticalAlignment="Center" Margin="6,0,0,0" />
                                </StackPanel>
                            </Border>
                            <ListBox x:Name="SurveyList"
                                     ItemsSource="{Binding Session.Surveys}"
                                     SelectedItem="{Binding Session.SelectedSurvey, Mode=TwoWay}"
                                     SelectionChanged="SurveyList_SelectionChanged"
                                     BorderThickness="0"
                                     MaxHeight="240"
                                     Background="Transparent"
                                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                     VirtualizingPanel.IsVirtualizing="True"
                                     VirtualizingPanel.VirtualizationMode="Recycling">
                                <ListBox.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <VirtualizingStackPanel />
                                    </ItemsPanelTemplate>
                                </ListBox.ItemsPanel>
                                <ListBox.ItemTemplate>
                                    <DataTemplate DataType="{x:Type vm:SurveyItemViewModel}">
                                        <StackPanel Orientation="Horizontal" Margin="0,2">
                                            <TextBlock Width="24" TextAlignment="Right" Margin="0,0,8,0"
                                                       Text="{Binding RouteOrder, TargetNullValue=•, FallbackValue=•}"
                                                       Foreground="{StaticResource TextTertiaryBrush}"
                                                       FontSize="{DynamicResource AppFontSize}" />
                                            <TextBlock Text="{Binding Name}"
                                                       FontSize="{DynamicResource AppFontSize}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </DockPanel>
                    </Border>
                    <!-- #476 Option C: optional manual position override. The
                         auto GPS marker is the zero-click default; this is the
                         "marker looks wrong / area uncalibrated" escape hatch. -->
                    <ContentControl Content="{Binding}" ContentTemplate="{StaticResource SetPositionAffordance}"
                                    HorizontalAlignment="Center" Margin="0,0,0,12" />

                    <Button Content="Go!" Style="{StaticResource WizardPrimaryButton}"
                            HorizontalAlignment="Center" Margin="0"
                            Command="{Binding MapOverlay.OptimizeRouteCommand}"
                            IsEnabled="{Binding SurveyFlow.CanOptimize}"
                            ToolTip="Compute the shortest path through all placed pins and start walking" />
                </StackPanel>

                <!-- Gathering -->
                <StackPanel Visibility="{Binding CurrentStep, Converter={x:Static controls:EnumMatchConverter.ToVisibility}, ConverterParameter=Gathering}">
                    <TextBlock Text="Walk to each target in order. We'll mark them collected automatically. New surveys you use along the way are added to the route too."
                               TextWrapping="Wrap" HorizontalAlignment="Center" TextAlignment="Center" Margin="0,0,0,16"
                               FontSize="{DynamicResource AppFontSizeBody}"
                               Foreground="{StaticResource TextSecondaryBrush}" />

                    <!-- Active-target summary: shows progress + the next pin's name out
                         here, since the map overlay no longer numbers individual pins. -->
                    <Border Background="{StaticResource AccentSoftBrush}" CornerRadius="3" Padding="12,8" Margin="0,0,0,20"
                            HorizontalAlignment="Center">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Visibility" Value="Visible" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Session.ActiveTargetSummary}" Value="">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <TextBlock Text="{Binding Session.ActiveTargetSummary}"
                                   FontWeight="SemiBold"
                                   FontSize="{DynamicResource AppFontSizeBody}"
                                   Foreground="{StaticResource AccentSoftTextBrush}" />
                    </Border>

                    <Button Content="Mark current collected" Style="{StaticResource WizardPrimaryButton}"
                            HorizontalAlignment="Center" Margin="0"
                            Command="{Binding ControlPanel.MarkCurrentCollectedCommand}" />

                    <!-- #476 Option C: correct a stale anchor mid-route. -->
                    <ContentControl Content="{Binding}" ContentTemplate="{StaticResource SetPositionAffordance}"
                                    HorizontalAlignment="Center" Margin="0,12,0,0" />
                </StackPanel>

                <!-- Done -->
                <StackPanel Visibility="{Binding CurrentStep, Converter={x:Static controls:EnumMatchConverter.ToVisibility}, ConverterParameter=Done}">
                    <TextBlock Text="Nice. Hit reset to start a new session."
                               TextWrapping="Wrap" HorizontalAlignment="Center" TextAlignment="Center"
                               FontSize="{DynamicResource AppFontSizeBody}"
                               Foreground="{StaticResource TextSecondaryBrush}" />
                    <Button Content="View report" Padding="14,6" Margin="0,16,0,0" HorizontalAlignment="Center"
                            Command="{Binding ViewLastReportCommand}"
                            Visibility="{Binding HasLatestReport, Converter={StaticResource BoolToVis}}"
                            ToolTip="Re-open the end-of-run summary (text + PNG card)" />
                </StackPanel>

                <!-- #113 Layer 4: derived Motherlode sub-steps. All input is
                     log-driven; these panels are read-only projections of the
                     coordinator snapshot. -->

                <!-- MotherlodeMeasuring — no readings yet. -->
                <StackPanel Visibility="{Binding CurrentStep, Converter={x:Static controls:EnumMatchConverter.ToVisibility}, ConverterParameter=MotherlodeMeasuring}">
                    <TextBlock Text="Click your metal-slab maps at three or more well-separated spots — distances are picked up automatically. After your first spot, the map overlay shows where to stand next (dashed ring). For best accuracy, log out and back in at each spot before using the maps."
                               TextWrapping="Wrap" HorizontalAlignment="Center" TextAlignment="Center" Margin="0,0,0,20"
                               FontSize="{DynamicResource AppFontSizeBody}"
                               Foreground="{StaticResource TextSecondaryBrush}" />
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="Oops — undo last check" Padding="14,6" Margin="0,0,8,0"
                                ToolTip="Accidentally checked a map? Reverse the last reading (multi-level)."
                                IsEnabled="{Binding Motherlode.CanUndo}"
                                Command="{Binding Motherlode.UndoLastReadingCommand}" />
                        <Button Content="Reset" Padding="14,6"
                                Command="{Binding Motherlode.ResetCommand}" />
                    </StackPanel>
                </StackPanel>

                <!-- MotherlodeLocating — readings in, nothing solved yet. -->
                <StackPanel Visibility="{Binding CurrentStep, Converter={x:Static controls:EnumMatchConverter.ToVisibility}, ConverterParameter=MotherlodeLocating}">
                    <TextBlock Text="Keep clicking your maps at new, well-separated spots — each treasure needs at least three measurement spots. Follow the dashed ring on the map (or the relative hint below) for the next best place to stand. Relog at each spot for the best fix."
                               TextWrapping="Wrap" HorizontalAlignment="Center" TextAlignment="Center" Margin="0,0,0,20"
                               FontSize="{DynamicResource AppFontSizeBody}"
                               Foreground="{StaticResource TextSecondaryBrush}" />
                    <ContentControl Content="{Binding Motherlode}" ContentTemplate="{StaticResource MotherlodeProgressBody}"
                                    HorizontalAlignment="Center" />
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,4,0,0">
                        <Button Content="Oops — undo last check" Padding="14,6" Margin="0,0,8,0"
                                ToolTip="Accidentally checked a map? Reverse the last reading (multi-level)."
                                IsEnabled="{Binding Motherlode.CanUndo}"
                                Command="{Binding Motherlode.UndoLastReadingCommand}" />
                        <Button Content="Reset" Padding="14,6"
                                Command="{Binding Motherlode.ResetCommand}" />
                    </StackPanel>
                </StackPanel>

                <!-- MotherlodeWalk — ≥1 treasure located: relative guidance +
                     the calibration-gated on-map marker (#113 Layers 1/3/5). -->
                <StackPanel Visibility="{Binding CurrentStep, Converter={x:Static controls:EnumMatchConverter.ToVisibility}, ConverterParameter=MotherlodeWalk}">
                    <TextBlock Text="Walk to each treasure. Locations are given relative to nearby landmarks, your map pins, and the spots you measured from — Project Gorgon has no coordinate readout. Maps are marked collected automatically as you dig them up."
                               TextWrapping="Wrap" HorizontalAlignment="Center" TextAlignment="Center" Margin="0,0,0,16"
                               FontSize="{DynamicResource AppFontSizeBody}"
                               Foreground="{StaticResource TextSecondaryBrush}" />
                    <ContentControl Content="{Binding Motherlode}" ContentTemplate="{StaticResource MotherlodeProgressBody}"
                                    HorizontalAlignment="Center" />
                    <!-- #113 Layer 5: real status, not an always-on nag.
                         Uncalibrated → offer the (optional) guided calibration
                         that unlocks the dot; the relative text above already
                         works without it. Calibrated → honest warp caveat. -->
                    <StackPanel HorizontalAlignment="Center" Margin="0,8,0,12"
                                Visibility="{Binding IsAreaCalibrated, Converter={x:Static controls:BoolToVisibilityConverter.Inverse}}">
                        <Button Content="Calibrate this area to show the map dot"
                                HorizontalAlignment="Center"
                                Command="{Binding CalibrateForMotherlodeCommand}"
                                ToolTip="Optional. The relative directions above are exact and need no calibration — this only adds a dot on the map overlay." />
                        <TextBlock Text="Optional — the relative directions above are exact without it."
                                   TextAlignment="Center" Margin="0,4,0,0"
                                   FontSize="{DynamicResource AppFontSizeSmall}"
                                   Foreground="{StaticResource TextTertiaryBrush}" />
                    </StackPanel>
                    <TextBlock Text="The on-map dot is approximate (the in-game map isn't perfectly to-scale); the relative directions above are exact."
                               TextWrapping="Wrap" HorizontalAlignment="Center" TextAlignment="Center" MaxWidth="520"
                               Margin="0,4,0,12"
                               FontSize="{DynamicResource AppFontSizeSmall}"
                               Foreground="{StaticResource TextTertiaryBrush}"
                               Visibility="{Binding IsAreaCalibrated, Converter={x:Static controls:BoolToVisibilityConverter.Instance}}" />
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,4,0,0">
                        <Button Content="Optimize Route" Style="{StaticResource WizardPrimaryButton}"
                                Margin="0" Command="{Binding Motherlode.OptimizeRouteCommand}"
                                ToolTip="Compute the shortest path between located treasures" />
                        <Button Content="Reset" Padding="14,6" Margin="12,0,0,0"
                                Command="{Binding Motherlode.ResetCommand}" />
                    </StackPanel>
                </StackPanel>

                <!-- MotherlodeDone — every located treasure collected. -->
                <StackPanel Visibility="{Binding CurrentStep, Converter={x:Static controls:EnumMatchConverter.ToVisibility}, ConverterParameter=MotherlodeDone}">
                    <TextBlock Text="All located treasures collected. Reset to start a new run."
                               TextWrapping="Wrap" HorizontalAlignment="Center" TextAlignment="Center" Margin="0,0,0,16"
                               FontSize="{DynamicResource AppFontSizeBody}"
                               Foreground="{StaticResource TextSecondaryBrush}" />
                    <ContentControl Content="{Binding Motherlode}" ContentTemplate="{StaticResource MotherlodeProgressBody}"
                                    HorizontalAlignment="Center" />
                    <Button Content="Reset" Style="{StaticResource WizardPrimaryButton}" Margin="0"
                            HorizontalAlignment="Center"
                            Command="{Binding Motherlode.ResetCommand}" />
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</UserControl>
